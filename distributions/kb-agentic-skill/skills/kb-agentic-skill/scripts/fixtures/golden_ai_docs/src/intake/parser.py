"""Parse a supplied order file, or refuse it with a reason."""


def load_accepted(path):
    """Return the internal order, or raise ValueError with a clerk-readable reason."""
    raise NotImplementedError
