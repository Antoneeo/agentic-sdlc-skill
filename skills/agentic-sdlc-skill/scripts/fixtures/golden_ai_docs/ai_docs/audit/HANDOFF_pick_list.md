# HANDOFF: pick_list (ephemeral — deleted at closure)

## Resume state
Branch `feat/pick-list`, builder skeleton committed, rendering not started.

## Next command
`python -m pytest tests/test_builder.py -x`

## Watch out
The two-widgets-one-name fixture is the whole point: do not simplify it away.
