---
description: The admission-test precedent ledger — every ruling ever made, keyed by the question the capability answers. Read BEFORE interpreting the Vision's prose.
status: CURRENT
---
# Admission rulings — the precedent ledger

**Why this file exists.** A prose gate re-interpreted from scratch on every proposal has
no memory: a rejected capability returns reworded and the game restarts. Here the game
does not restart. A capability's identity is **the question it answers / the effect it
has in the delivered system — never its vocabulary**: a restatement is a paraphrase,
and a paraphrase lands on the same row, which already carries its verdict.

**The procedure** (it precedes the prose, `project_vision.md` §The admission test):

1. **Place** the proposal against this table: does it answer the same question as a
   row? Semantic judgement, same discipline as kb's taxonomy pass.
2. **EXISTS → the row's verdict applies.** To claim otherwise, write the **distinction
   line** — "it answers a different question because …" — falsifiable, on the record.
   If the distinction line cannot be written, it is the same capability (the sibling
   rule). Silent re-interpretation is not a path that exists.
3. **MISSING → the prose rules once**, and the verdict becomes a new row. A proposal
   matching both an ADMIT and a REJECT row is **CONTESTED → the owner rules, with the
   basis recorded**. The machine never decides.

A working bypass is not a Vision failure to rewrite around — it is a new row. The
ledger only grows; rows are superseded by an owner ruling, never deleted.

| id | the capability answers the question / has the effect | verdict | deciding line | source | basis |
|---|---|---|---|---|---|
| r1 | counts, caps or meters how much a user may produce or validate — however framed, including as a fix | REJECT | the user's guarantee; disclosure names the metered unit | M89 (real); v8 round A | — |
| r2 | collects per-document or per-work state into one surface — stored, generated or derived on demand; sorted or not | REJECT | Non-Goal 1 (work-management), the aggregation clause | readiness board (real, v6 era); v8 round B; F-024 coverage distribution | — |
| r3 | tells an agent or user what to work on next, in what order, or by whom | REJECT | Non-Goal 1, stated by the question answered | Non-Goal 1's own definition; kb `mentions:` work-queue (refused in F-024) | — |
| r4 | widens an admitted capability to reach more work, people or product than admitted | re-ruled as new | §Admission test, scope growth | v8 round C | — |
| r5 | amends the Vision inside the proposal the amendment authorizes | REJECT | amendments ruled first and apart; Non-Goals never amendable by a capability proposal | v8 round D | — |
| r6 | ships, in a sibling or paid layer, what a Non-Goal forbids | REJECT | Non-Goals bind every skill in the family, present or future | v8 round E | — |
| r7 | adds a user-visible ability under a maintenance/perf label | not exempt; ruled fresh | the exemption's effect backstop | v8 round F | — |
| r8 | approaches a Non-Goal's territory and does not disclose the deciding fact | REJECT | omission resolves against the proposal | v8 round G | — |
| r9 | outputs the set of documents that are NOT current (or any selection by state) | REJECT | the manifest discriminator: a lookup about a document you name is in; "what to do next" is out | v8 round H | — |
| r10 | introduces a second triage authority over work something here already governs | REJECT | Non-Goal "one triage authority per kind of work" | round 8 pm-sibling attack | — |
| r11 | consolidates duplicated internal machinery into one authored source | ADMIT | the North Star's one-core family; the maintenance exemption | F-022 (real) | — |
| r12 | governs a domain of work the family does not reach, with its own fidelity discipline, adding no capability the family lacks | ADMIT | Goal 7; the proposal names the practitioner and their work today | kb sibling (real); v8 P2 | — |
| r13 | makes the methodology reachable from one more AI client | ADMIT | the client carve-out: the rule does not reach the clients | v8 P3 | — |
| r14 | fixes a defect with purpose, actors and surface unchanged | exempt | the bounded maintenance exemption | v8 P4; F-053 (real): nine items placed here — an unpaid scoped-re-review debt, a human-approval rule absent from one package, r23's own pointer delivered to one lens of three, a report printing an inverted claim, a probe rotting by construction, a probe that could not fail, figures reproducing under no ruler, two citations resolving to the wrong thing, a harness decoding without an encoding. None answers a new question; the two that grow a mandatory contract disclose their bytes and rest on the owner's port confirmation of 2026-09-11, not on r23, whose cost text priced the rule and never its per-lens carrier | — |
| r15 | packaging, installation, distribution, the product's own tests | ADMIT | part of the product, not overhead | v8 P6 | — |
| r16 | specifies the observable actor-surface interaction binding use cases to surfaces BEFORE the solution is designed | ADMIT | advances "divergence visible before implementation" and the Actors' good-UX clauses; ceremony cost disclosed in full (conditional L3 section + doctrine reading cost) and explicitly accepted by the owner | F-032 (real); content model evolved to the flow in v2 (2026-08-05) — same capability, no re-ruling | owner acceptance 2026-08-04 (v1) + 2026-08-05 (v2 ceremony), ANALYSIS_interaction_contract.md Diary |
| r17 | specifies the complete observable behavior (rules, cases, acceptance criteria) binding use cases to outcomes BEFORE the solution is designed — component-free, readable by the owner as the "is this what we want?" object | ADMIT | advances "divergence visible before implementation": the WHAT becomes one validated object instead of an implicit synthesis across documents; sibling of r16 (r16 = interaction geometry, r17 = behavior semantics — the distinction line is the authority split); ceremony cost (one conditional section, one bundle projection) disclosed and accepted | F-033 (real, owner design conversation) | owner acceptance 2026-08-06, ANALYSIS_functional_spec.md |
| r18 | at a due review gate, a mandated one-question stop when the only independent rung is permission-gated — no grant memory, per-gate re-ask, five-bullet form | ADMIT | advances "divergence visible before implementation/merge" and "surface the conflict, let the user choose"; ceremony cost disclosed in full (one question per gate per intact context, unbounded only in context losses + one reason word on below-rung-1 rows) and explicitly accepted by the owner | F-038 (real); successor to F-037, CANCELLED at the review cap after grant memory failed three ways | owner acceptance 2026-08-25 (cost) + 2026-08-26 (implementation at the cap), ANALYSIS_gated_rung_vocabulary.md |
| r19 | injects a constant one-line reminder into EVERY user prompt, whose first duty is to decide whether the skill governs the work and, if it does, load it once | ADMIT | an unread protocol cannot be applied, and nothing else reaches a session that never loads the skill (`orient` fires once and decays); the "no ceremony ratchet" Non-Goal's second door — cost disclosed in full and accepted by the owner. Bounded to stay flat: constant text, zero reads, one line under 500 chars, guarded by a battery; a trivial turn pays a yes/no judgement, never a load. Carrier consolidated from kb's F-041 item B rather than duplicated (r11) | F-046 (real): a release ran in this repo against a `GUIDE_release.md` the session never consulted, because SKILL.md had not been read; two process reports the same day answered with a local defect instead of the mechanism | owner acceptance 2026-09-06 (four lines + per-turn cost, then default wiring), HANDOFF_per_turn_protocol_reminder.md |
| r19 | tells the agent to offer subagents whenever they would be useful, outside the review gates | REJECT | an open-ended licence to interrupt; fires outside the gates; converts a precise doctrine into a habit — ruled with r18 as its declared twin | F-038 Non-Goal | — |
| r20 | at a due review gate, constrains WHICH CAPABILITY TIER may run it — a floor below which a role may not start, with disclosure when the only independent rung sits under it | ADMIT | advances the anti-myopia North Star at the gate itself: a review run below the capability its judgement needs certifies the omission it was there to catch. Declared twin of r18, distinction line on the record: **r18 governs whether an available rung may be USED (permission); r20 governs whether a usable rung is CAPABLE ENOUGH (capacity)** — a client can satisfy r18 in full and still run a design review with a model that cannot do it. Tiers stay client-relative capability levels, never provider names. Ceremony cost disclosed in full (one `model` cell per REVIEW_LOG row, no read, no new step; L1 zero) and explicitly accepted by the owner | F-048 (real); external evidence: cascade routing pays only where a threshold signal exists (FrugalGPT, RouteLLM), which is why economy is legal for dispatch and not for a judgement gate | owner acceptance 2026-09-10 (ceremony cost), ANALYSIS_capability_tiers.md |
| r21 | states which passes may leave the authoring context and which never may — a classification keyed on whether the pass's output is falsifiable and on its read/report ratio | ADMIT | advances the North Star from the other side: delegated judgement is unfalsifiable, and the never-list is what keeps authoring where its wrongness can still be caught. Near-miss on **r3** disclosed per r8, distinction line on the record: **r3 rejects a surface that orders or assigns work; r21 orders nothing, schedules nothing and assigns to no one** — it classifies a pass the agent has ALREADY decided to run. Adds no field, no step and no artifact; it constrains a choice the orchestrator was making anyway | F-048 (real); external evidence: multi-agent costs 3-15x tokens and buys context isolation, not savings, so the boundary is framed on falsifiability rather than cost | — |
| r22 | reports, on demand, what the review gates have already caught and what the doctrine costs to read — an aggregate over CLOSED review events, carrying no verdict and naming no work item | ADMIT | advances Goal 3 by making the product's central claim computable instead of asserted, and Goal 4 by printing the read-cost term beside it so neither half can be quoted alone. Near-miss on **r3/r2** disclosed per r8, distinction line on the record: **r2 and r3 forbid a surface reporting the state of WORK — open items, their readiness, what to do next; r22 reports the outcome of events that already happened, names no `doc_key` as needing anything, and its output changes only when a review is LOGGED, never when work is added or finished.** If the owner reads that line as too fine, the correct verdict is CONTESTED and the ruling is theirs. The r15 adjacency was considered and dropped: r15 covers the product's own tests, which carry a verdict, and this is defined by never carrying one (always exits 0, never a CI gate). Ceremony: nothing becomes mandatory — no artifact, no field, no check, no gate touched; L1/L2/L3 pay zero, so the ratchet's acceptance door is not needed | F-050 (real): the process had produced 590 verified findings across 63 reviews with a stated moment, 68% of them before any code existed, and that number had never been computed in four months | — |
| r23 | extends the capability floor from the GATES to the SESSION that authors — a floor a session cannot route around, satisfied by disclosure, plus a forbidden combination (authoring below floor AND reviewing below floor on one unit) | ADMIT | advances the anti-myopia North Star at the point r20 could not reach: r20 constrains a rung the AGENT chooses at a gate, where routing is the remedy; r23 constrains the session the USER chose before the agent existed, where routing is impossible and the only remedies are disclosure and compensation — the falsifiable distinction from r20, on the record (r4: scope growth of an admitted capability is re-ruled as new). Fails open by construction: a session that cannot determine its own tier owes nothing, because an agent cannot acquire a capability it was not given. Ceremony cost disclosed in full (one conditional disclosure at L3 authoring, fired only where the session CAN determine its tier and it is below floor; no new field — it reuses `model` from r20; L1 and L2 pay zero) and explicitly accepted by the owner | F-049 (real): the owner asked how an agent invoking the skill from a user-set model behaves and which model to set; grounding the question against `review.md` showed the floor was titled "which tier may run this GATE" and named no authoring role, though authoring is the purest case its own criterion describes | owner acceptance 2026-09-11 (ceremony cost), ANALYSIS_authoring_floor.md |
