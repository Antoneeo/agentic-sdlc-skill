# Vision Roadmap
Status: DRAFT

| Milestone | Expected Benefit | Priority | Success Signal | Status |
|:---|:---|:---|:---|:---|
| Vision Governance | Prevent strategic drift during agent-led development | High | `ai_docs/vision/` is created by default and consulted before feature analysis | [COMPLETED] |
| Agentic SDLC vNext | Keep the skill autonomous while adding proportional triage, mechanical checks, and devPNT milestone Vision alignment | High | Runtime skill folder includes support files and installer covers Claude, Codex, and Gemini | [COMPLETED] |
| Vision Actors | Characterize the actors in the Vision so UX is designed for named roles, not an implicit "user" | Medium | Vision templates carry `## Actors`; each use-case attaches to a defined actor; devPNT M-VISION mirrors it (amplification when devPNT is present, not required) | [COMPLETED] |
| Agent Evolution (operative guides, subagent execution, devPNT coexistence seam) | An agent starting with no prior context inherits the project's model instead of rebuilding it, and the process is one and the same whether devPNT is present or not | High | The guide layer is shipped and consumed: `reference/GUIDE_*.md` with the generated guide router (`reference/INDEX.md` — it reports guide matches and derives no triage level; the level comes only from the skill's single triage step, and the consult verdict is declared alongside it), checkable cold via `evals/scenarios/consult_fires_on_match.md` and `verdict_declared_on_no_match.md`; the skill's coexistence contract records one authoritative home per artifact when devPNT is present, and without devPNT the skill is authoritative everywhere | [COMPLETED] |
| Verifiable Vision | The Vision Gate becomes testable: a reviewer with no project context can rule ACCEPT/REJECT on a proposal quoting one line of the Vision, instead of trusting the author's intent | High | `project_vision.md` is APPROVED only after blind adversarial rounds; the standing battery (`audit/reviews/BLIND_VISION_REVIEW_2026-07-27.md`) is re-run before any promotion to APPROVED and on every edit that changes what a Vision commits to — a typo or formatting fix stays one step — each run recorded in that file and in `audit/reviews/REVIEW_LOG.md` | [COMPLETED] |
| Architect Pass + Design Review Gate | Designs are ruled at capability level before files are named, and independently reviewed before any code — myopia caught at design time, not at closure | High | Every L3 ANALYSIS carries a Capability Ledger; the design-gate review is logged in `audit/reviews/REVIEW_LOG.md` before implementation starts — both additions land at L3 with their cost stated and accepted by the owner (ceremony budget in `solutions/ANALYSIS_architect_pass.md`, accepted 2026-07-28) | [COMPLETED] |
| Multi-Domain Core (F-022) | One authored source of process discipline, three distributions (`agentic-sdlc`, `kb-agentic`, `mkt-agentic-sdlc`): maintenance stops scaling with the number of domains, and a fourth domain is an overlay, not a fork | High | One repository publishes three packages; spine divergence between the distributed copies is caught mechanically by the drift-guard check; `npm pack` is correct for all three; each sibling restates the three level boundaries in its own domain's units before its package publishes | [COMPLETED] |
| kb Knowledge Method (F-024, consuming the Claim Ledger F-025) | kb answers the question it exists for — what do we already know about this, how sure are we, and where did it come from — with conflicts detected and held open, resolved only by new information, never by a silent guess | High | A real corpus, named at acceptance time, ingested end to end with kb's `sdlc_check.py` graph and corpus checks clean; figures the corpus itself states about its subject (effort, duration, resources as reported in the sources, each with its source — never estimates, rankings or schedules attached to this project's own units of work) and evaluation answers (confirms / refines / contradicts, conflicts surfaced) produced from the graph rather than by re-reading the documents; the owner's acceptance recorded in the kb ANALYSIS Diary | [IN_PROGRESS] |

## Notes
- Vision documents sit above strategic architecture docs: they explain why the project exists and where it should not go.
- Architecture, analysis, and tests remain mandatory at L2 and L3, interpreted through the Vision Gate; L1 is unchanged — make it, run the existing tests.
- Status describes each milestone itself. This table is a "milestones and sequencing" home `project_vision.md` names under *Where the rest lives*, alongside the evolution roadmap (`vision/roadmap_evoluzione_agenti.md`). Every row names a milestone already delivered or already under way. The table states no order among open rows and answers no question about which document or unit of work is taken up next; per-document state stays in each document's own frontmatter. Priority records the ranking assigned when each milestone was framed — historical context, not an ordering among open rows.
- Row status is a dated snapshot, last updated 2026-08-01; the frontmatter status above is this document's own lifecycle.
- F-numbers resolve in `strategic/features_history.md` (generated); per-feature detail lives in `solutions/ANALYSIS_*.md`. The three F-022 packages are built and unpublished as of 2026-08-01; when they publish is the owner's decision, and this table states no condition an agent can evaluate.
