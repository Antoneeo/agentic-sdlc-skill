# Independent Review Log

Records each independent-review-gate pass (§4.5 tech artifacts, §4.6 code). This is how the gate's real benefit is measured over time — one row per review.

| date | doc_key | tier | reviewer | findings_raised | findings_real | verdict | revise_rounds |
|---|---|---|---|---|---|---|---|
| 2026-07-02 | p_tm_operative_guides | deep | devpnt-tech-reviewer | 7 | 7 | PASS | 2 |
| 2026-07-02 | e_isp_operative_guides_u1 | deep | devpnt-tech-reviewer | 10 | 10 | PASS | 2 |
| 2026-07-02 | e_tdd_operative_guides_u1 | light | devpnt-tech-reviewer-light | 4 | 4 | PASS | 3 |
| 2026-07-02 | impl unit-1 diff vs E-TDD | code | devpnt-code-reviewer (deep; implementer: sonnet, economy tier) | 2 | 2 | PASS | 1 |
| 2026-07-02 | adr_2026-07-02_client_relative_model_tiers | light | devpnt-tech-reviewer-light | 2 | 2 | PASS | 1 |
| 2026-07-02 | GUIDE_release vs release-runbook snapshot | guide | general-purpose (fresh context, read-only) | 2 | 2 | PASS | 3 |
| 2026-07-02 | p_tm_execution_disciplines | deep | devpnt-tech-reviewer | 1 | 1 | PASS | 1 |
| 2026-07-02 | e_isp_execution_disciplines | deep | devpnt-tech-reviewer | 2 | 2 | PASS | 2 |
| 2026-07-02 | e_tdd_execution_disciplines | light | devpnt-tech-reviewer-light | 2 | 2 | PASS | 1 |
| 2026-07-02 | impl M2 diff vs E-TDD | code | devpnt-code-reviewer (deep; implementer: sonnet, economy tier) | 1 | 1 | PASS | 1 |
| 2026-07-02 | e_isp_agent_kb_u2 | deep | devpnt-tech-reviewer | 2 | 2 | PASS | 2 |
| 2026-07-02 | e_tdd_agent_kb_u2 | deep (escalated from light: security design) | devpnt-tech-reviewer | 3 | 3 | PASS | 2 |
| 2026-07-02 | impl unit-2 diff vs E-TDD | code | devpnt-code-reviewer (deep; implementer: sonnet, economy tier) | 2 | 2 | PASS | 1 |
| 2026-07-03 | p_tm_subagent_execution | deep | devpnt-tech-reviewer | 4 | 4 | PASS | 2 |
| 2026-07-03 | e_isp_subagent_execution | deep | devpnt-tech-reviewer | 5 | 5 | PASS | 2 |
| 2026-07-03 | e_tdd_subagent_execution | deep (escalated from light: T1 critical RCE invariant) | devpnt-tech-reviewer | 3 | 3 | PASS | 2 |
| 2026-07-03 | impl M3 diff vs E-TDD | code | devpnt-code-reviewer (deep; implementer: sonnet, economy tier) | 2 | 2 | PASS | 1 |
| 2026-07-03 | adr_2026-07-03_executable_plan_json_in_md | light | devpnt-tech-reviewer-light | 0 | 0 | PASS | 1 |
| 2026-07-03 | p_tm_consolidation | deep | devpnt-tech-reviewer | 3 | 3 | PASS | 2 |
| 2026-07-03 | e_isp_consolidation_hook | deep | devpnt-tech-reviewer | 2 | 2 | PASS | 1 |
| 2026-07-03 | e_tdd_consolidation_hook | light | devpnt-tech-reviewer-light | 0 | 0 | PASS | 1 |
| 2026-07-03 | impl Unit-1 diff vs E-TDD | code | devpnt-code-reviewer (deep; implementer: Opus orchestrator, same-session, no dispatch) | 1 | 1 | PASS | 1 |
| 2026-07-03 | e_isp_consolidation_guide_consumption | deep | devpnt-tech-reviewer | 2 | 2 | PASS | 2 |
| 2026-07-03 | e_tdd_consolidation_guide_consumption | light | devpnt-tech-reviewer-light | 0 | 0 | PASS | 1 |
| 2026-07-03 | impl Unit-2 diff vs E-TDD | code | devpnt-code-reviewer (deep; implementer: Opus orchestrator, same-session) | 1 | 1 | PASS | 1 |
| 2026-07-03 | e_isp_consolidation_eval_harness | deep | devpnt-tech-reviewer | 3 | 3 | PASS | 2 |
| 2026-07-03 | e_tdd_consolidation_eval_harness | light | devpnt-tech-reviewer-light | 2 | 2 | PASS | 2 |
| 2026-07-03 | impl Unit-4 diff vs E-TDD | code | devpnt-code-reviewer (deep; implementer: Opus orchestrator, same-session) | 2 | 2 | PASS | 1 |
| 2026-07-03 | adr_2026-07-03_skill_eval_harness | light | devpnt-tech-reviewer-light | 0 | 0 | PASS | 1 |

## Notes
- **adr_2026-07-03_skill_eval_harness** (M4 closure): round 1 PASS, zero findings. Reviewer verified well-formed ADR (all sections), the Decision matches the built harness (static battery = unittest discover of 3 test files, zero LLM/network/subprocess; behavioral corpus opt-in/non-CI), the 3 rejected alternatives are the real ones (LLM-in-loop / pure-static / mandatory-GitHub-Actions), no overclaim (live-adherence correctly framed human-judged/non-gating), no provider names (client-relative-tiers ADR honored), .github/ confirmed absent.
- **impl Unit-4** (M4, eval harness — 8 files, real Python + prose): same-session implementation. Battery green: `unittest discover` = 51 tests (plan 32 + orient 9 + skill-invariants 10) OK; driver smoke rc0 (valid) / rc2 (malformed, fail-fast T5); check --hybrid CLEAN. Deep code review PASS, zero BLOCK: diff touches EXACTLY the 8 E-ISP files; REPO=parents[3]/SKILL_DIR=parents[1] correct; all 10 test methods match the E-TDD anchors (verified present in live docs); test_indexes_idempotent calls only build_index/build_manifest/build_guide_index (never cmd_index — no repo mutation), mapping matches; run_behavioral.py genuinely stdlib-only (zero forbidden tokens → T4 by construction); scenarios parse under load_scenario; static battery deterministic (T9). 2 WARNs, both resolved: (1) [conformance] test_support_files_wired used a hardcoded expected-list + `*.md` glob instead of the E-TDD's dangling-regex — ACCEPTED as a justified deviation (the impl mechanism is more robust: the E-TDD's regex would false-fail on bare ai_docs doc names referenced in SKILL.md like README.md/INDEX.md/handoff.md; the two primary invariants — orphan-exists-unreferenced + expected-deleted — ARE both guarded; the residual gap is only a dangling ref to a NEW nonexistent file outside the list, a narrow tertiary case). Shadow reflects the impl mechanism. (2) [security] _confined missed Windows drive-relative paths (`C:foo`) — HARDENED post-review per the reviewer's suggested fix (added `bool(p.drive)` reject); battery re-run 51/51 green. Both bounded by P-TM T5 (scenarios are repo-trust, opt-in/non-CI).
- **e_tdd_consolidation_eval_harness** (M4 Unit 4): round 1 FAIL — 2 BLOCK. BLOCK1 (process order): the parent E-ISP was still a PENDING proposal (not yet user-accepted → not in the retrievable doc store), so the E-TDD had no parent to diff against; the orchestrator got ahead of the sequence (correct order = E-ISP accepted → then E-TDD). Resolved by having the user accept the E-ISP, then re-reviewing. BLOCK2 (real code bug): `REPO = parents[2]` off-by-one — `parents[2]` is `skills/`, not the repo root where ai_docs/ lives; fixed to `parents[3]` (+ `SKILL_DIR = parents[1]` for skill-doc reads), matching the proven sibling test_plan.py:229. Round 2 PASS zero findings: reviewer fetched the now-accepted E-ISP and confirmed the 8-file Module Change Plan maps 1:1 (no leak/miss), parents[3] arithmetic correct, all asserted anchors present verbatim, builder mapping real (build_index:286/build_manifest:393/build_guide_index:457, cmd_index sole writer), P-TM T4/T5/T9/T10 mechanisms carried. Process lesson: finalize a derived E-TDD only after its parent E-ISP is accepted.
- **e_isp_consolidation_eval_harness** (M4 Unit 4, eval harness): round 1 FAIL — 1 BLOCK + 2 WARN. BLOCK: the static release-gate battery's asserted-invariant SET was incomplete — it guarded U2 consult/proactive but omitted U3 worktree-hygiene bullets (SKILL.md Phase 4/5) and the ENFORCEMENT §4 hook doc; blast-radius even claimed worktree was asserted (self-contradiction). A green gate that can't catch a worktree/hook regression IS the anecdotal-regression gap the milestone exists to close. Fixed: battery responsibility now enumerates ALL M4 outputs (U1 orient+§4 hook / U2 consult+proactive+dispatch note / U3 worktree Phase 4/5) and the map+blast-radius+cross-consistency agree on the same set. WARN1: stale line ref `## Mechanical Enforcement` :264→:273. WARN2: anti-orphan "pointer resolves" must regex ALL backtick body pointers (header :12-16 = 4 files + inline :180/198/201/204/209 = 5 more), not just the header list. Round 2 PASS zero findings: reviewer confirmed build_index:286/build_manifest:393/build_guide_index:457 return strings (no-write idempotence buildable), evals/ outside the validator's ai_docs/ roots, .github/ absent (opt-in grounded, no over-correction), all anchors real, P-TM T4/T5/T9/T10 carried. Also: defines "demonstrably" (Fable finding #1) as a runnable seeded-repo scenario + static presence assertion.
- **Unit-3 (worktree hygiene) — independent review SKIPPED by scope (recorded).** Deliberate light-path call approved by the user: the unit is L2-scale prose (2 bullets in SKILL.md — isolate an L3 on its own branch/worktree in Phase 4; explicit merge decision + cleanup in Phase 5, referencing the devPNT .db-lock reality). Governance already covered by D-UC UC4 + P-TM (declared Unit 3 "no new attack surface"), and no E-TDD exists → the §4.6 code-review gate is optional by its own scope rule ("L1/L2 with no E-TDD has no spec to conform to"). Verification = orchestrator self-review (aligned with M-VISION signal 3 + the .db-lock operational constraint, no DRY/contradiction) + closure gate `check --hybrid` CLEAN. No separate E-ISP/E-TDD authored (proportionality per Rule Zero — avoided over-governing a localized doc-only edit).
- **impl Unit-2** (M4, guide consumption discipline — prose, 3 files): same-session prose implementation; closure gate check --hybrid CLEAN (same 4 baseline warnings). Deep code review PASS, zero BLOCK: diff touches EXACTLY the 3 E-ISP files (guides.md/SKILL.md/dispatch.md, no leak/miss); placement conforms (guides.md §0 before §1, §Proactive inside §1; SKILL.md 3-moment lead ADDED with all 4 existing Operative-Guides paragraphs preserved, §4 consult bullet, §5 proactive bullet; dispatch.md note by the loop). P-TM bounds survived into prose: T7 (L1-exempt + targeted-match-never-blanket) in the consult prose; T6 (PROPOSE-not-create / never-silent / never-model-knowledge / distilled_from-intact) in the proactive prose. DRY grep-confirmed: router-scan mechanics single-sourced in guides.md §0, SKILL.md + dispatch.md POINT (no second copy); consult prose names BOTH project + agent-KB routers; no existing instruction inverted. 1 cosmetic WARN accepted: dispatch.md parenthetical reads "the same broad final pass below" vs E-TDD's "Phase-5/review.md pass" — semantically equivalent, an in-file cross-reference to dispatch.md's own closure line, reviewer judged it arguably better; left as-is.
- **e_tdd_consolidation_guide_consumption** (M4 Unit 2): round 1 PASS, zero findings, no escalation. Light reviewer verified all 3 E-ISP files covered 1:1 in the Module Change Plan; anchors exact (guides.md `## 1` at :20 ending :35 before `## 2` :37 — room for the insertions; SKILL.md `## Operative Guides` :246 / `### 4` :193 / `### 5` :204; dispatch.md `guides`/`plan brief` :26-28); the 4 "keep unchanged" Operative-Guides paragraphs are exactly as claimed (:248-261); P-TM T6 (propose-only/user-confirmed/no-model-knowledge/distilled_from intact) + T7 (targeted-match/no-blanket/triage-proportional/L1-exempt) both carried into the drafted prose; DRY holds (SKILL.md + dispatch.md point, don't restate router-scan); state-model N/A honest; TDD exemption cites the real SKILL.md:197 rule; check --hybrid real (SKILL.md:100/147). No off-by-N citation errors.
- **e_isp_consolidation_guide_consumption** (M4 Unit 2, guide consult + proactive-creation triggers): round 1 FAIL — 1 BLOCK + 1 WARN, both real cross-feature interactions. BLOCK: `dispatch.md` missing from the impact map — the consult trigger interacts with M3 subagent dispatch (a context-free subagent reads guide PATHS from `plan brief`'s `guides` field and cannot run its own router lookup), so the consult trigger in the dispatch path = populating each task's `guides` at plan-authoring time; the subagent does NOT self-consult. Fixed: dispatch.md added to the map (MODIFY, a short reconciling note; mechanics stay single-sourced in guides.md — no DRY break). WARN: confirm the single §5 Closure proactive hook governs both same-session and dispatch closure (shared Phase-5/review.md) — added that cross-consistency line. Round 2 PASS zero findings: reviewer verified plan-authoring `guides`-field mechanics (sdlc_check.py:912-1000), dispatch.md:29/:52, the 3-file map is exact (no over-correction), templates.md/Rule Zero/INDEX.md correctly not-touched, P-TM T6/T7 wording carried, M-VISION signal-4 aligned. Value: caught a real M3↔M4 interaction the author's context missed — guide consumption already flows through the dispatch guides-by-pointer path.
- **impl Unit-1** (M4, SessionStart orientation hook): same-session implementation by the Opus orchestrator (well-scoped 1-module change, full context — subagent dispatch not used). Battery green: test_session_start.py 9/9 + test_plan.py 32/32 regression + smoke (repo-root 4 sections / empty-dir silent rc0 / --hybrid pointer); check --hybrid CLEAN (4 pre-existing baseline warnings only: roadmap/principles DRAFT, functional-docs missing-status carried debt). Deep code review PASS, zero BLOCK: diff touches EXACTLY the 4 E-ISP files (no leak, package.json/lib.js correctly untouched); cmd_orient(args) matches E-TDD verbatim; T1 zero-exec traced across the full call graph (no subprocess/eval/git_* reachable) + dynamic sentinel test; T2 total cap arithmetic bounds emitted body to exactly 16000 (boundary traced, no off-by-one); T3 every path via confine_under with None-skip; T8 no non-zero return path + find_project_root non-raising; T10 CI-copy note present; tests genuinely assert their named invariants; purely additive (no existing dispatch altered), DRY-clean. 1 non-blocking WARN accepted as declared residual: read_text loads the whole doc before slicing (read-side unbounded) — repo-trust-bounded, P-TM T2 caps EMITTED output (satisfied), and read_text behaves identically at all 5 existing call sites, so a local-only bounded-read would be an inconsistent deviation; not fixed for M4.
- **e_tdd_consolidation_hook** (M4 Unit 1): round 1 PASS, zero findings, no escalation.
- **e_tdd_consolidation_hook** (M4 Unit 1): round 1 PASS, zero findings, no escalation. Light reviewer verified every anchor against sdlc_check.py: insertion point (early dispatch after :1034 `return cmd_gate`, before root-resolution :1036) exact; `common`/`hybrid_opt` supply `--root`/`--hybrid` as claimed; `confine_under`/`read_text`/`find_project_root` bodies confirmed non-spawning (zero-exec T1 holds by reuse); `except Exception: return 0` fail-open matches 5 existing precedents in the file (incl. cmd_gate's payload parse ~:786) — not a novel risk; SKILL.md `### 1. Audit and Alignment` at :155 confirmed; ENFORCEMENT §3 at :27 → new §4 placement correct; package.json files has no test entry (dev-only test confirmed). All 4 E-ISP files covered 1:1 (no leak/omission), all 5 P-TM [MILESTONE] reqs (T1/T2/T3/T8/T10) each have a design mechanism + a named test, state-model N/A honestly discharged. Escalation to deep judged unwarranted: security invariants close by direct reuse of already-verified helpers + hard-coded path list (no user/content-derived input), mechanically simpler than the M3 cmd_plan RCE surface.
- **e_isp_consolidation_hook** (M4 Unit 1, SessionStart orientation hook): round 1 PASS, 2 cosmetic WARNs folded before proposing. (a) release triple-bump (package.json version / gemini-extension.json / CHANGELOG) excluded — reviewer noted M3 e_isp_subagent_execution *listed* them; kept the exclusion but added an explicit "deliberately deferred to release unit M4.A6, and Unit 1 ships nothing new via the allowlist" note so the deviation is legible, not forgotten. (b) blast-radius mis-located the fail-open surface ("never let find_project_root raise") — reviewer verified find_project_root (sdlc_check.py:94) returns cwd and never raises; reworded to "every fixed doc path is optional, missing → skip → exit 0". Reviewer independently re-derived the impact map and confirmed no missing file: the stale .codex/hooks.json is a test fixture (leave), entry_points.md is stale devPNT boilerplate (not a curated catalog), package.json files unchanged (orient ships inside listed sdlc_check.py; test dev-only), all 5 Unit-1 [MILESTONE] P-TM reqs carried, --hybrid honors "no bootstrap duplication".
- **p_tm_consolidation** (M4, all four units): round 1 FAIL — 1 BLOCK + 2 WARN. BLOCK = the recurring overclaim antipattern: T1/T4/T5/T8/T9 asserted security invariants in present tense as existing enforcement, but the SessionStart hook + eval harness are unbuilt (verified: `scripts/` holds only sdlc_check.py + test_plan.py, no `.github/workflows`, no eval/scenario code) — same failure that FAILED `p_tm_operative_guides`. Fixed with a Build-state legend + per-threat `[EXISTING]` vs `[MILESTONE]` tags and requirement voice ("the hook/eval unit MUST…"), mirroring accepted M3 P-TM. WARN1 = missing threat: CI gate-script/workflow integrity (consumers copy sdlc_check.py per ENFORCEMENT.md §2; a drifted copy → false-confidence gate) → added T10, bounded at repo-trust level like T2 (accepted residual + document-the-assumption requirement, no fabricated guard). WARN2 = Unit 3 (worktree prose) undeclared no-surface → explicit "no new attack surface" declaration added. Round 2 PASS zero findings: reviewer independently verified every `[EXISTING]` anchor against source (`confine_under` fail-closed single source sdlc_check.py:112 reused 5 sites; `cmd_plan` zero-execution call graph w/ module-level subprocess for git only; guides.md provenance keys; router ≤160-char descriptions; ADR 2026-07-02 accepted) and confirmed every `[MILESTONE]` unit genuinely unbuilt. Value: prevented shipping a threat model that would let the human trust unbuilt hook/eval guards as existing; the [MILESTONE] tags now drive the M4 E-ISP/E-TDD security requirements.
- **impl M3** (subagent execution): 4th live model-per-dispatch run — economy implementer (Sonnet) from the E-TDD shadow brief, 7 files, self-run battery 32/32, validate rc0 with the same 4 pre-existing baseline warnings (behavior-preserving confinement refactor proven), check --hybrid CLEAN, zero-execution smoke (a `verify: "touch PWNED"` task left no file). Deep code review PASS zero BLOCK: T1 traced across the full cmd_plan call graph (no subprocess/git_*/eval reachable, verify only print()-ed), both confinement sites behavior-preserved ((ValueError, OSError) with both .resolve() inside try; site-1 error+continue AND "target not found" warning; site-2 "outside root" error), signatures match E-TDD, _confine_or_reject/_validate_plan_tasks are faithful decomposition. 2 declared-acceptable WARNs left as-is: (a) ledger orphan-id warning gated behind `if not errors` — moot since an invalid plan already fails rc2/no-dispatch; (b) the symlink-escape test re-hits the `..` guard rather than a literal post-.resolve() escape (hard to construct portably; the `..`/absolute branches ARE covered). Note (not M3): devPNT regenerated ai_docs/functional/{architecture_overview,external_interfaces}.md and stripped their lifecycle frontmatter → the 2 baseline "missing status" warnings; restore at a devPNT-owned functional-docs pass.
- **E3 design chain M3** (subagent execution): the independent review gate caught 3 real BLOCKs at design time before the economy implementer ran — P-TM T1 invariant misstated ("no subprocess import" is false; subprocess is module-level for git → reframed as a cmd_plan call-graph property), E-ISP missing impacted file (README.md enumerates support files), E-TDD confine_under not behavior-preserving (ValueError-only would let an OSError that was a fail-closed rejection become an uncaught crash). All three would have shipped a latent bug or a false security claim. Gate cost: 3 artifacts × 2 rounds each.
- **impl unit-2** (agent-global KB): third live model-per-dispatch run — economy implementer (Sonnet), battery 12/12 including the symlink-escape resolve branch via NTFS junction fallback (symlink needs admin on this machine; fallback pre-authorized by the E-TDD scenario, reason recorded) and the no-KB byte-identical regression with baseline captured BEFORE the code edit. Deep code review PASS zero BLOCK: 6/6 E-ISP files exact, sdlc_check blocks verbatim, edge cases re-derived (empty overrides, empty KB, OSError safety, rc machinery), T6 confinement verified with distilled_from parity. 2 declared-acceptable WARNs: guides.md +10 vs +9 estimated budget (spec baseline miscalibrated); POSIX-absolute overrides on Windows caught by the resolve guard instead of the string guard (same nuance as the distilled_from precedent, still fail-closed).
- **e_tdd_agent_kb_u2** (Feature B unit 2, escalated light→deep per §4.5 escalation rule — security design): round 1 FAIL, 2 BLOCK both on battery coverage of declared security requirements — (a) the symlink-escape resolve branch (`relative_to` ValueError, the exact T6 vector) was untested (scenarios 4/5 only hit the string guards); (b) the E-ISP-declared "distilled_from on KB root via --root" scenario was missing. +1 WARN: singular `overrides:` limitation undeclared. Round 2 PASS (1 informational note, no action). Reviewer validated both design decisions: env seam AGENTIC_SDLC_KB_ROOT as test/CI-only (product surface stays the fixed path — elicitation honored) and overrides-equals-colliding-filename semantics; verified insertion point, rc machinery, signatures, and confinement parity against the real code.
- **e_isp_agent_kb_u2** (Feature B unit 2): round 1 FAIL — 1 BLOCK: the draft declared "T6 unchanged / no new input surface" while introducing `overrides:` — untrusted frontmatter resolved against the KB root, exactly the Boundary-3 traversal vector P-TM T6 tags [MILESTONE] for this unit. Fixed: T6 declared EXTENDED, overrides confined with distilled_from parity (absolute/`..`/symlink escape → ERROR fail-closed; confined-but-missing → warning). +1 WARN: DEFAULT_KB_ROOT is new code (Path.home() zero occurrences — cross-platform is an assertion, battery covers it) and the root==KB self-comparison edge (collision must no-op). Round 2 PASS zero findings. Reviewer verified the distilled_from precedent semantics (out-of-root ERROR / missing warn) and that glob-confined list_guides closes the collision-read surface.
- **impl M2** (execution disciplines): second live run of model-per-dispatch — economy implementer (Sonnet) from the E-TDD shadow brief, battery self-run 8/8; deep code review PASS first round, zero BLOCK (conformance exact: 9/9 E-ISP rows, 6/6 SKILL.md touch points verbatim, AAA single-home confirmed by 0-hit grep, versions untouched, all MUSTs load-bearing with why). 1 cosmetic WARN: E-TDD skeleton said "H2 sections" including Applicability, implementation renders applicability as lead line — E-ISP's "applicability line FIRST" governs, left as-is. Files came in 45-59 lines vs ~90 ceiling.
- **e_tdd_execution_disciplines** (M2): round 1 PASS with 2 WARN, both folded before proposing — (a) the draft invented a "hard cap 95" ceiling nowhere in the M-VISION (~90): battery now verifies ≤~90, overage only with stated reason; (b) present-tense "reused by the Hybrid gates" contradicted the E-ISP's own "may later point" scoping: rephrased as intended-for-reuse, devPNT-side wiring out of unit. Reviewer verified 9/9 E-ISP rows covered, all SKILL.md anchor lines accurate, pointer budget honest, state-model N/A honest, MUSTs load-bearing with why.
- **e_isp_execution_disciplines** (M2): round 1 FAIL — 1 BLOCK: unit framed as v1.9.0 but package.json `version` bump unstated and gemini-extension.json (always version-locked) absent from the map → implementing as written ships a version desync. Fixed by scoping version bumps OUT of the unit explicitly (GUIDE_release step 1 owns all three bump points — reviewer verified the guide text authorizes exactly this split). +1 WARN: templates.md line-168 AAA hint survives outside the declared single home — now acknowledged as deliberate scaffold placeholder. Round 2 PASS, no new omissions. Reviewer independently verified all NOT-touched claims (validator reads only ai_docs/; lifecycle scripts don't enumerate support files) and all cited SKILL.md line numbers.
- **p_tm_execution_disciplines** (M2): round 1 PASS with 1 WARN — missing threat: an orphaned/mis-anchored discipline file (ships, passes budget+DRY checks, but never wired into its SKILL.md phase → silently never fires, failing its own success signal). Evidence from the M-VISION success signals' wiring points. Folded as T8 before proposing. Reviewer independently verified every claim (allowlist precedent, GUIDE_release battery, ADR tiers, roadmap C6, enforcement ladder, evidence base) — all held; tags honest, no Non-Goal violation.
- **GUIDE_release** (first field guide, guides.md §5 review): round 1 PASS with 2 WARN — smoke command lost the `<repo>/` prefix in compression (misleading as written); publish step lacked its inline §Publish pointer (marker hygiene). Both folded into v2 together with the user's amendment (commit+tag+push via `git_push_tag.bat`); source re-snapshotted (hash a1044dca), delta re-review PASS with zero findings. Value: the pipeline's guide-vs-source pass catches synthesis-time distortion, not just invention. Round 3 (v3, hash ba876688, v1.8.1 session): script re-run trap added from field observation — delta PASS, zero findings.
- **impl unit-1** (Feature B / M1): first live run of model-per-dispatch — economy-tier implementer (Sonnet) working from the E-TDD shadow as a self-contained brief; deep code review PASSED first round with zero BLOCK (conformance exact on all E-ISP components, all three cmd_stale return conversions correct, T1/T2/T6/T8/T9 verified, needle resolution intact). 2 optional WARNs recorded as follow-up candidates, both documented-as-intended: (a) orphaned guide router (guides removed, INDEX.md left) only warns in cmd_index and is invisible to validate; (b) cmd_stale hashes distilled_from without re-running T6 confinement (validate already fails such a guide; stale alone is not fail-closed). Orchestrator ran the full §5 battery independently: 9/9 green + absolute-path variant.
- **e_tdd_operative_guides_u1** (Feature B / M1, unit 1): round 1 FAIL — 1 BLOCK: the cmd_stale restructure claimed the audit-plan body could stay "unchanged" with rc OR-ed in, but its three early returns would silently discard a drifted-guide rc (e.g. drifted guide + empty audit plan ⇒ false success); fixed by showing the rewritten tail verbatim (`return rc` on both early paths). +2 WARNs (DEPRECATED scoping note; --strict is a blanket end-of-run mechanism, not per-warning escalation). Round 2 caught off-by-2 line citations in the fix itself (implementer brief precision). Round 3 verification-only PASS. Value: a real control-flow bug killed at design time, before the cheap-tier implementer could inherit it.
- **e_isp_operative_guides_u1** (Feature B / M1, unit 1): round 1 FAIL caught 2 BLOCK + 3 WARN — (1) `.sources/` "excluded (dotdir)" was FALSE (`list_canonical_docs` rglob has no dot-filter; snapshots would be swept into the manifest) → became a real Impact Map row; (2) index-model inconsistency (UC-5/F3 promise `reference/INDEX.md`, nothing generates it) → resolved by generating it and dropping the hand router. Round 2 PASS with 5 WARNs folded before proposing: GENERATED_DOCS no-op removed, T2 drift moved from validate to `stale` (honors the M-VISION "one freshness motor" — a silent lodestar deviation the reviewer caught), two-index role split declared, description≤160 constraint, lodestar status alignment (M-VISION v1.1).
- **p_tm_operative_guides** (Feature B / M1): round 1 FAIL caught 5 BLOCK + 2 WARN — 3 overclaims (T1 provenance/marker validation, T2 source_version drift, T5 overrides/collision were stated as existing enforcement but are unbuilt milestone deliverables) and 2 missing threats (T6 guide-path traversal, T8 provenance spoofing). Round 2 PASS after NOW/MILESTONE/PROCESS tagging + the two threats added. Concrete value: prevented shipping a threat model that would have let the human trust mechanical guards that do not exist yet. The [MILESTONE] tags now drive the E-ISP/E-TDD security requirements.
