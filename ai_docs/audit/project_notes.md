Repo is CRLF (edit as content-delta). devPNT off — Standalone. Standing Vision
battery: `audit/reviews/BLIND_VISION_REVIEW_2026-07-27.md`, re-run on every Vision edit.

**Published on npm (verified `npm view` 2026-08-05):** code 1.21.1, kb 1.4.1,
mkt 0.4.1. `v1.16.0` was tagged and never published — it stays as history, and no
pushed tag is ever moved.

**Prepared and unpublished: code 1.22.0, kb 1.4.2, mkt 0.4.2** (F-032 Interaction
Contract + registry-recognition/battery-isolation fixes). Bumped 2026-08-05, all four
bump points per package + CHANGELOG ×3; verification battery green (pack ×3, init
smoke, check, unittest ×3). Remaining: tag+push (`git_push_tag.bat`) and `npm publish`
×3 (2FA, owner's act) per `GUIDE_release.md`. Companion workstream owed in the devPNT
repo: the governed D-IC artifact (sequence D-UC → D-IC → P-TM → E-ISP).
