# Handoff — workstream registry
Date: 2026-08-03 (UTC)

| Workstream | Level | Branch | Status | Since | Next step | Details |
|---|---|---|---|---|---|---|
| F-015 Code-Comprehension Guides | L3 | main | PAUSED | 2026-07-19 | dogfood #8: write one real `source_kind: code` guide | ANALYSIS_comprehension_guides.md (Diary; no volatile state) |
| Release 1.19.0 (Architect Pass + Design Review Gate) | — | feat/architect-pass (tag v1.19.0) | AWAITING OWNER | 2026-07-28 | merged to main; `npm publish` (2FA) is the remaining step — verify `npm view` → 1.19.0. It carries 1.18.0, never published. Then field-test on large brownfield projects: `evals/scenarios/architect_rules_before_impact.md` + `unmapped_never_grounds_missing.md` are the cold-run harness | CHANGELOG `[1.19.0]`/`[1.18.0]` · ANALYSIS_architect_pass.md · ANALYSIS_design_review_gate.md |

| F-022 Multi-Domain Core | L3 | feat/multi-domain-core | DONE, HELD | 2026-07-31 | Complete and consolidated: one repository, the code distribution at the root, kb and mkt under `distributions/` grafted with `git subtree` (history preserved; mkt's TAGS did not transfer and still live only in the old clone + GitHub). Batteries green, three golden transcripts intact, drift guard comparing the copies to each other, `npm pack` correct for all three. **Release HELD by the owner (2026-08-01): nothing is published until kb is effective** — see F-024. Nothing else outstanding here | ANALYSIS_multi_domain_core.md |


| Field test: the skills exercised by agents, orchestrator as the user | — | main | DONE | 2026-08-01 | closed 2026-08-02 — nothing left | **All three trials passed, and each found something a static pass had not.** code: implemented per-user rate limiting, then found that the feature itself REMOVED protection (a forgeable token prefix + authenticated traffic no longer consuming the IP bucket), bounded it, said the bound does not restore the baseline, and refused to mark the node COMPLETED with its own ship-gate open. kb: 8 topics / 55 claims with verified locators, 13 CONTESTED rows held symmetrically, ruling requested instead of decided; its reviewer FAILed the first draft for resolving a conflict by inference. mkt: all four validators clean, 13 FACT rows matching exactly what the user stated, the user's own objective quantified as unreachable and returned as a three-branch fork; its reviewer caught a circularity of authority. Three product defects found and fixed in F-027 (lens identity, FACT laundering, verdict return path) | ANALYSIS_field_test_defects.md · REVIEW_LOG 2026-08-02 |
| Release of the three packages | — | feat/kb-knowledge-method | AWAITING OWNER | 2026-08-01 | **F-024/F-025 CLOSED** (COMPLETED 2026-08-01: method + ledger implemented, three design-gate rounds disposed, acceptance run on the Eclosion corpus CLEAN, the capacity conflict resolved by owner ruling with basis, ADR recorded). The hold on F-022's release was "until kb is effective" — kb now does its job on real documents; **publishing (npm ×3, 2FA) remains the owner's act**. mkt tags v0.2.0/v0.2.1 still live only in the old clone + GitHub. **Versions to publish: code 1.20.3, kb 1.0.1, mkt 0.3.1** (1.20.2/1.0.0/0.3.0 were published on 2026-08-02 and carry the F-027 defects; kb and mkt shipped a routing note naming the wrong lens) — code moved off 1.20.1 because that tag is already pushed at a commit whose README was wrong (missing `routing.md`/`sdlc_core.py`, licence line contradicting MIT); never move a pushed tag. `gemini-extension.json` was also stuck at 1.19.0 (code) and 0.2.1 (mkt) — the third bump point had been skipped for two releases, now aligned | ANALYSIS_kb_knowledge_method.md · ANALYSIS_claim_ledger.md · ADR_2026-08-01_kb_topic_graph_claim_ledger.md |

| F-026 Question Discipline | L3 | claude/agitated-blackburn-62f5cf (worktree `romantic-pascal-8d15af`, cut from `feat/kb-knowledge-method`@ed1d65a) | DONE, AWAITING MERGE | 2026-08-01 | owner's field defect: agents ask useless questions. Complete — legality test in `elicitation.md` reachable from Rule Zero, non-blocking default with the same evidence duty as a question, blocking form generalized from F-025. Two design-review rounds (round 1: 6 findings; round 2 re-run: 9, incl. 3 working evasions), all fixed; three distributions green, drift guard identical, `validate` 0 errors, wiring invariant mutation-tested. **Merge is the owner's call and is the only thing left**: the branch is 2 commits ahead of its base but `feat/kb-knowledge-method` has since moved to 5e61170, so the two have diverged — rebase onto it, or cherry-pick 50c5cf7+a0072cf onto main. F-026 is independent of F-024; merging into the kb branch makes it ride F-024's release hold. Afterwards: delete the worktree, then kb adopts the section inside F-024 and mkt owes it too | ANALYSIS_question_discipline.md |

| F-030 Portable knowledge (kb export/import) | L3 | main | DONE, AWAITING OWNER | 2026-08-03 | complete: `export`/`import`, `prov: IMPORTED` (owner ruled option A), `portability.md`, eval scenario, batteries 141/204/159 OK, drift identical, `check` CLEAN. **kb 1.2.0 published 2026-08-03** (shasum `47e9c86c…` = local pack; 23 files). The closure review caught `portability.md` missing from `package.json` — it would have shipped doctrine the user cannot open — now guarded by a new shared invariant. `portability.md` §0 carries the two literal invocations and the four sentences to say to the user, with an executable spec driving them through `main()` so doctrine and CLI cannot drift. code 1.20.3 / mkt 0.3.1 unchanged in what they ship. **Still open, unnumbered: `explaining.md`** — the owner's idea, shaped as an explaining discipline (sibling of `elicitation.md`), never a user manual: it names no command and lists no feature, so it cannot diverge from what is enforced. (F-031 went to exhaustive ingestion instead, by owner priority) | ANALYSIS_kb_portable_knowledge.md · REVIEW_LOG 2026-08-03 |
| F-031 Exhaustive ingestion | L3 | main | DONE, AWAITING OWNER | 2026-08-03 | complete: the north star above the rules in `distillation.md`, `extracted_through:` on the sidecar with its three checks, bounded reading windows (one `PLAN_` task each — no second register), a coverage cell for EVERY artifact in `corpus/INDEX.md` (r9), cold-run scenario, batteries 141/218/159 OK, drift identical, `npm pack` 23 files, `check` CLEAN. **kb 1.3.0 published 2026-08-03** (`npm view` = 1.3.0, shasum `b32ad830…` identical to the local pack, 23 files) and tagged `kb-v1.3.0` on `9556f2c`, pushed — never move that tag. code 1.20.3 / mkt 0.3.1 unchanged in what they ship. **Breaking for existing corpora on purpose**: `check` errors on every artifact with claims and no `extracted_through:`, and `corpus/INDEX.md` needs one `index` run — both stated in the two changelogs. Also fixed in passing: the SHIPPED kb CHANGELOG was three releases stale (a user installing 1.2.0 read 1.0.1 as the latest) | ANALYSIS_kb_exhaustive_ingestion.md · REVIEW_LOG 2026-08-03 |
| F-028 Mergeable documentation | L3 | main | PROGRESS | 2026-08-02 | design review DONE (round 1 FAIL, 5 findings incl. 3 BLOCK, all folded; round 2 PASS) -- the gate the plan named is passed. Next: Action Plan A, the generator itself (sources + generated view + alignment check), with the four facts the review pinned: `Date:` from the newest frontmatter VALUE never an mtime, stable sort, refuse-to-write on unaccounted rows, cap as a warning. Nothing implemented, no volatile state | ANALYSIS_mergeable_docs.md (Diary) - REVIEW_LOG 2026-08-03 |
| F-029 kb field-report defects | L3 | main | DONE, PUBLISHED | 2026-08-02 | **kb 1.1.0 published 2026-08-03** (`npm view` = 1.1.0, shasum `d763434e…` identical to the local pack, so the registry carries the exact tested tree). A–G implemented, batteries 140/190/158 OK, drift identical, `check` CLEAN. **kb ONLY**: code (1.20.3) and mkt (0.3.1) are unchanged in what they ship — the shared invariant lives in `test_skill_invariants.py`, which no tarball contains; do not bump or republish them. `evals/` is dev-only and does not ship, so the three new cold-run scenarios never reach users. **kb 1.1.1 published 2026-08-03** (shasum `b6c041ca…` = local pack), closing three doctrine-vs-machinery inconsistencies the same practitioner found by reading 1.1.0: `SKILL.md` never naming `anchor`, the Write Triggers row still carrying §1's pre-1.1.0 wording, and `anchor` resolving paths only from inside the docs root. Both releases tagged at the tree the registry carries — `kb-v1.1.0` on `50f5c88`, `kb-v1.1.1` here; never move either. Local installs on this PC verified at 1.1.0's build (42568 bytes, `--help` listing the overlay) — re-run `npm i -g @antoneeo/kb-agentic-skill@latest` to pick up 1.1.1 | ANALYSIS_kb_field_report.md · REVIEW_LOG 2026-08-02 |

## Project-wide notes
Repo is CRLF (edit as content-delta). devPNT off — Standalone. Standing Vision battery:
`audit/reviews/BLIND_VISION_REVIEW_2026-07-27.md` (re-run on every Vision edit).
**Published on npm (verified 2026-08-02): code 1.20.2, mkt 0.2.1, kb 1.0.0** — the 1.20.3/1.0.1/0.3.1 patch release is prepared and unpublished. Release branches are cut from the previous
release tag and not yet merged to main: `feat/architect-pass` carries 1.17.0's content
plus 1.18.0. `v1.16.0` was tagged and never published — stays as history.
